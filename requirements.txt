# MUTCD Multimodal RAG — HPRC-friendly pinned requirements
# Tested with Python 3.10 / 3.11 on TAMU HPRC (Grace, FASTER, Launch, ACES).
# Install order matters when using a fresh conda env — install torch first
# matching your CUDA module, then the rest.

# ----- Core ML stack -----
# Install torch separately first with the right CUDA wheel, e.g. for CUDA 12.1:
#   pip install --index-url https://download.pytorch.org/whl/cu121 \
#       torch==2.4.1 torchvision==0.19.1
# Leaving torch unpinned here so the explicit install above wins.
torch>=2.3,<2.6
torchvision>=0.18,<0.21

# ----- HF + retrieval -----
transformers>=4.49,<4.55
accelerate>=0.33,<1.5
sentence-transformers>=3.0,<4.0
safetensors>=0.4.3
huggingface_hub>=0.24,<1.0
# CLIP tokenizer in sentence-transformers needs these on some installs
ftfy>=6.1
regex>=2024.4

# Qwen2.5-VL helpers (image/video preprocessing)
qwen-vl-utils>=0.0.8

# ----- PDF + images -----
pymupdf>=1.24,<2.0          # imported as `fitz`
pillow>=10.0,<12.0
numpy>=1.26,<3.0
tqdm>=4.66

# ----- UI -----
gradio>=4.44,<6.0

# ----- Jupyter kernel + widgets -----
# ipykernel is needed for `python -m ipykernel install --user --name mrag`
# so the env shows up in the JupyterLab kernel switcher.
ipykernel>=6.29
ipywidgets>=8.1
